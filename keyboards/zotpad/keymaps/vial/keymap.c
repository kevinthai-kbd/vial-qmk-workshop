#include "ZOTpad.h"

const uint16_t PROGMEM keymaps[][MATRIX_ROWS][MATRIX_COLS] = {

  [0] = LAYOUT( /* Layer Zero */

		KC_P1, 	KC_P2,  KC_P3,
		KC_P4, 	KC_P5, 	KC_P6,
		KC_P7, 	KC_P8, 	KC_P9

		),
  

  [1] = LAYOUT( /* Layer One */
		KC_TRNS, KC_TRNS, KC_TRNS,
		KC_TRNS, KC_TRNS, KC_TRNS,
		KC_TRNS, KC_TRNS, KC_TRNS
		),

  [2] = LAYOUT( /* Layer Two */
		KC_TRNS, KC_TRNS, KC_TRNS,
		KC_TRNS, KC_TRNS, KC_TRNS,
		KC_TRNS, KC_TRNS, KC_TRNS
		),

  [3] = LAYOUT( /* Layer Three */
		KC_TRNS, KC_TRNS, KC_TRNS,
		KC_TRNS, KC_TRNS, KC_TRNS,
		KC_TRNS, KC_TRNS, KC_TRNS
		),
  
	
};
